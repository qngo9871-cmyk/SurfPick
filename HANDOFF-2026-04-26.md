# Surf Pick — Session Handoff 2026-04-26

## 1. Decisions made this session

- **Hand-managed Xcode project, not XcodeGen** — Surf Pick is past setup phase; migration cost outweighs benefit for this app.
- **For all future iOS/macOS/watchOS projects, evaluate XcodeGen at kickoff** — saved to memory.
- **Use Google Maps as default for the coordinate-fix checklist** — Q's preference over Apple Maps; community-contributed surf markers tend to be more accurate.
- **Dataset fix strategy = Option B + Option C combined** — tighten the auto-patch algorithm (B) AND hand-curate local Ballina-area spots from Q's verified Google Maps coords (C).
- **Patch threshold for auto-fix is 2 km** — spots within 2 km of OSM result are left alone, only larger errors get patched.
- **Order of remaining work** — dataset fix first, then Google Maps URL scheme, paywall + tier split, widget extension, info screen + Report wrong location, then App Store assets.

## 2. Design ideas discussed but not decided

- **Whether to also include OSM-found patches with tighter algorithm (Option B), or rely only on Q's hand-curated coordinates (Option C)** — Q said "do option b and c" but the v2 algorithm script was never executed; it was started then aborted when Q shifted to "find more local spots too."
- **Which spots from the proposed "missing" list (Wategos, Tallows, Broken Head, Boulder Beach, Lighthouse Beach Ballina, South Wall, Brunswick Heads, Suffolk Park) actually belong in the dataset** — checklist generated, awaiting Q's input.
- **Whether the generic "Ballina" entry should be renamed to a specific break (e.g. Lighthouse Beach, South Wall)** — flagged in checklist, not resolved.
- **Whether the generic "Lennox Head" entry should be renamed to a specific break (e.g. The Point, Main Beach, Boulders)** — flagged in checklist, not resolved.

## 3. Files created or modified this session

- `/Users/user/SurfPick/local_spots_checklist.md` — **created** then **edited**. Markdown checklist with 10 existing spots near Ballina + 8 proposed missing spots. Originally Apple Maps instructions, then edited to use Google Maps instructions per Q's preference.
- `/tmp/verify_spots.py` — **created**. First-pass OSM verification script. Failed — used `bounded=1` viewbox which returned 0 matches.
- `/tmp/verify_spots2.py` — **created**. Second-pass without bounding box. Also failed — Python urllib SSL cert issue on macOS returned 0 matches silently.
- `/tmp/verify_v3.py` — **created**. Third-pass using `subprocess.run(['curl', ...])` instead of urllib. Worked. Verified 30 random spots; median error 1.61 km, 73% within 3 km, 15% with errors >5 km.
- `/tmp/fix_au_spots.py` — **created** and **executed**. Auto-patcher for Australian spots, threshold 2 km. Generated 17 patches.
- `/tmp/surf_spots_fixed.json` — **created** by fix_au_spots.py. Patched dataset with 17 Australian spots updated. **Not deployed** — multiple bad patches identified (Flat Rock patched to Currumbin 80 km away, Forster patched to Scotts Head 171 km away, North Point patched to Rottnest 191 km away, several patches matched OSM surf shops/SLSCs/motels instead of breaks).
- `/tmp/au_patch_report.json` — **created** by fix_au_spots.py. Per-spot patch report.
- `/tmp/fix_au_v2.py` — **partially written** then **aborted mid-write**. Tighter algorithm with BAD_NAME_KEYWORDS, BAD_CLASSES, GOOD_CLASSES filters and 50 km max patch distance. Was being written when Q redirected to "find more local spots too." Incomplete and not saved fully.

No source files (Swift, Package.swift, Info.plist, project.pbxproj) were modified this session.

## 4. Files proposed but not yet created

- `/Users/user/SurfShared/Sources/SurfShared/Resources/surf_spots.json` (replacement) — pending Q completing the checklist. Will be a hand-curated patch over the existing 627-spot file, using Q's verified coords for ~10 existing local spots and ~8 new local spots.
- A complete `/tmp/fix_au_v2.py` — tighter auto-patcher. Aborted partway through writing. Whether to finish this depends on whether Q wants Option B applied to the rest of Australia after the local checklist is done.

## 5. Code written in chat but not saved to disk

`/tmp/fix_au_v2.py` (incomplete fragment, partially written before being aborted):

```python
"""
Tighter auto-patcher for Australian surf spots.

Rejects patches where:
- OSM result name contains shop/club/motel keywords (we want geographic features, not businesses)
- OSM result is >50km from claimed coord (likely wrong-region match)
- OSM result type is amenity/shop/leisure (Nominatim's `type` and `class` fields)

Accepts patches where OSM result is a beach, bay, headland, locality, or natural feature.
"""
import json, math, time, subprocess, urllib.parse

DATASET_PATH = '/Users/user/SurfShared/Sources/SurfShared/Resources/surf_spots.json'
OUTPUT_PATH = '/tmp/surf_spots_fixed_v2.json'
PATCH_THRESHOLD_KM = 2.0
MAX_PATCH_DISTANCE_KM = 50.0  # don't accept patches that move the spot >50km

# Reject results whose display_name contains these (case-insensitive)
BAD_NAME_KEYWORDS = [
    'surf shop', 'surf co', 'surf australia', 'surf life saving',
    'motel', 'hotel', 'cafe', 'restaurant', 'pizzeria',
    'school', 'church', 'pharmacy', 'medical', 'clinic',
    'caravan park', 'camping', 'reserve office',
    'club house', 'lifesaving club', 'rsl', 'bowling club',
]

# Reject results whose Nominatim `class` field is one of these
BAD_CLASSES = {'amenity', 'shop', 'leisure', 'tourism', 'office', 'craft', 'healthcare'}

# Accept results whose `class` is one of these (geographic features)
GOOD_CLASSES = {'natural', 'place', 'waterway', 'boundary', 'landuse'}

def haversine_km(lat1, lon1, lat2, lon2):
    R = 6371.0
    dlat = math.radians(lat2 - lat1); dlon = math.radians(lon2 - lon1)
    a = math.sin(dlat/2)**2 + math.c
```

(Cuts off mid-`math.cos(...)` — never completed. Do not run as-is. Either finish writing the function and the rest of the patching loop, or rewrite from scratch.)

## 6. Open questions

- **Has Q filled in the local_spots_checklist.md?** — file was created but Q has not yet completed it or said "checklist done."
- **For the auto-patch results, which of the 17 spots in `/tmp/au_patch_report.json` are correct vs wrong?** — partial review done in chat (categorized into "likely correct," "likely wrong," "definitely wrong") but no final keep/reject decisions saved.
- **Whether to ship Surf Pick with `surf_spots.json` containing only Q's verified local spots + the existing global spots untouched, or to also run the tightened Option B algorithm against the rest of Australia/world before shipping** — depends on Option B re-run, which hasn't happened.

## 7. Potential problems flagged

- **15% of the 627-spot dataset has coordinate errors >5 km, 27% have errors >3 km** — concrete evidence: `/tmp/verify_v3.py` output showed median error 1.61 km, max error 14 km (Phillip Island Woolamai), with examples Flat Rock Ballina off by 1.4 km and Port Macquarie Lighthouse off by 5.9 km. **Same dataset is used by the live `Surf Near Me` watch app.**
- **526 of 627 spots have suspiciously round 3-decimal-place coordinates (LLM rounding fingerprint)** — concrete evidence: Python analysis script counted exact 3-decimal-place values, output `Spots with 3-decimal-place coords (suggestive of LLM rounding): 526/627`.
- **Auto-patch results from `/tmp/fix_au_spots.py` contain wrong-region matches** — concrete evidence: Flat Rock (Ballina) patched to Currumbin Gold Coast (80 km), Forster (One Mile Beach) patched to Scotts Head (171 km), North Point (WA) patched to Rottnest Island (191 km). Saved to `/tmp/au_patch_report.json`. **Do not deploy `/tmp/surf_spots_fixed.json` as-is.**
- **Auto-patch results match OSM business entries instead of breaks** — concrete evidence in chat-side review: Margaret River → "Gnarabup surf shop", Coffs Harbour Diggers → "Coopers Surf Australia", Newcastle Beach → "Surf Beach Motel", Port Macquarie Lighthouse → "Surf Life Saving Club", Gunnamatta → "Surf Life Saving Club". Recorded in `/tmp/au_patch_report.json`.
- **Debug `print()` in `LocationManager.swift` — must be removed before App Store submission** — concrete evidence: edit was made this session at the `locationManager(_:didUpdateLocations:)` callback, prints `📍 SurfPick GPS fix:` with lat/lon/accuracy/age. File path: `/Users/user/SurfShared/Sources/SurfShared/LocationManager.swift`. (Line number not captured this session — verify in CLC.)
- **Python's `urllib` on macOS fails SSL cert validation silently** — concrete evidence: `/tmp/verify_spots2.py` returned 0 matches for every query while `curl` returned valid JSON for the same URL (e.g. Maroubra). Workaround: use `subprocess.run(['curl', ...])` for HTTP. Speculative cause: Python 3.13 on macOS doesn't trust the system root certs by default — speculative — needs verification in CLC.
- **The watch app `Surf Near Me` may be giving misleading rankings to users with the same flawed dataset** — concrete evidence: `/Users/user/surfcheck/SurfCheck/surf_spots.json` is byte-identical structure to the file copied into SurfShared. Whether it's currently causing user-visible issues is speculative — needs verification in CLC by checking watch app reviews or testing in person.
